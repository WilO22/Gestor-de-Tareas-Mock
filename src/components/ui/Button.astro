---
// 1. Recibimos las props: el enlace (href) y el texto (text)
const { href, text } = Astro.props;
---

<a href={href} class="bg-blue-500 text-white px-4 py-2 rounded-md hover:bg-blue-600">
    {text}
</a>